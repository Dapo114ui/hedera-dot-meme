import { createAdapter } from '@buidlerlabs/memejob-sdk-js/adapter'; import { MJClient, getChain, ContractId, CONTRACT_DEPLOYMENTS } from '@buidlerlabs/memejob-sdk-js'; import { JsonRpcProvider } from 'ethers'; async function t() { const adapter = createAdapter('EvmAdapter', { ethereumProvider: new JsonRpcProvider('https://testnet.hashio.io/api') }); const client = new MJClient(adapter, { chain: getChain('testnet'), contractId: ContractId.fromEvmAddress(0, 0, CONTRACT_DEPLOYMENTS.testnet.evmAddress) }); try { const token = await client.getToken('0.0.5271847'); console.log(Object.keys(token)); console.log(Object.getOwnPropertyNames(Object.getPrototypeOf(token))); } catch(e){console.log('Error', e)} } t();  
